import React from 'react';
import cn from 'clsx';

interface Props {
  className?: string;
}

export const SplideSlider: React.FC<Props> = ({ className }) => {
  return (
	<div className={cn('', className)}>
	  dvf
	</div>
  );
};
